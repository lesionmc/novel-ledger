# 02 · 需求清单（novel-ledger）

> 本文档为项目复盘补写，事实依据见 README/代码/记忆日志（2026-09-03 工作日志）。
> 优先级定义：**P0** = 不做则产品不成立（核心闭环）；**P1** = 做了才好用/能交付；**P2** = 规划中未开工。
> 信息属性：**明确** = 拍板人明确说过；**承诺** = 已对外承诺（README/开源仓库）；**推断** = 从代码与日志反推；**待验证** = 需确认才能定稿。
> 状态：**已实现** = 代码里有且实测跑通；**部分实现** = 有代码但有已知缺口；**未实现** = 无代码。

---

## 1. 需求条目表（共 30 条）

| 编号 | 需求描述 | 优先级 | 验收标准（Given-When-Then / 可验证一句话） | 信息属性 | 状态 |
|---|---|---|---|---|---|
| R1 | **单章连写**：按「创作宪章 + 大纲 + 角色卡 + 账本 + 上一章结尾 800 字」配方生成指定章节正文 | P0 | Given 书目录含三件套且已建账，When 执行 `write_chapter.py --book X --chapter N`，Then 生成 `chapters/chNNN.md` 且达到目标字数（默认 3000，可用 `--words` 1000–10000 自定义，见 R31）、控制台打印 `[3/4] 正文已落盘` | 明确 | 已实现 |
| R2 | **滚动状态账本**：全书唯一 `story_state.md`，固定 7 小节（当前时间/计数与资源/角色状态/关键事件时间线/伏笔账本/关键物件/待续状态） | P0 | Given 账本已存在，When 打开它，Then 7 个 `##` 小节标题与 `STATE_TEMPLATE` 逐字一致，一个不多一个不少 | 明确 | 已实现 |
| R3 | **建账** `--init-state`：按三件套生成初始账本 | P0 | Given 新书只有三件套，When 执行 `--init-state`，Then 生成 `story_state.md` 且内容与设定一致（当前时间=故事起点，无编造事实） | 明确 | 已实现 |
| R4 | **账本自动更新**：写完一章后调模型「旧账 + 本章正文」覆盖式重写全账 | P0 | Given 写完第 N 章，When 主流程跑到 `[3/4]` 之后，Then 账本被整体刷新且打印 `账本已更新 → …story_state.md`；第 50 章与第 2 章的账本体积同量级（不随章数线性膨胀） | 明确 | 已实现 |
| R5 | **账本防幻觉规则**：正文与旧账冲突时必须写 `⚠冲突：旧账记X，本章写Y（请作者裁决）`，不许悄悄覆盖 | P0 | Given 正文与账本存在时间/计数/物件矛盾，When 更新账本，Then 对应小节下出现一条 `⚠冲突` 条目 | 明确 | 已实现（`STATE_UPDATER_PROMPT` 规则 9） |
| R6 | **账本只补不删**：未了结的角色状态/伏笔必须保留 | P0 | Given 旧账有"待回收"伏笔，When 连续写 5 章，Then 该伏笔条目仍在（状态可能改为"已回收·回收于第X章"），不得消失 | 推断 | 已实现（规则 4/6） |
| R7 | **去 AI 腔红线（预防端）**：7 条 DEAI_RULES 注入 SYSTEM_PROMPT，生成时即自检 | P0 | Given 红线已注入，When 写一章，Then 该章 hard 命中数 ≤ 前一版本同章，且破折号 < 10 处/章 | 明确 | 已实现（实测 ch6 hard 3→0、破折号超标→达标） |
| R8 | **L1 词表硬筛** `--scan`：本地零 token，出各章 AI 腔指数 + hard/soft 命中 + 标点密度 + 跨章意象复用 | P0 | Given 书目录有章节，When 执行 `deai.py --scan X`，Then 控制台打印每章指数与命中清单，且**不发任何网络请求**（断网可跑） | 明确 | 已实现 |
| R9 | **L2 AI 精判** `--polish`：单章出人读报告 + 机读 diff JSON，**不改正文** | P0 | Given 已 scan，When 执行 `--polish X --chapter N`，Then 落盘 `chNNN.AI腔体检.md` 与 `chNNN.AI腔diff.json`，且正文 `chNNN.md` 修改时间不变 | 明确 | 已实现 |
| R10 | **安全应用** `--apply`：只应用 hard 级改写，应用前自动备份原稿 | P0 | Given 已生成 diff JSON，When 执行 `--apply X --chapter N`，Then 生成 `chNNN.bak.md` 且打印 `成功应用 N 处 / 跳过 M 处`；soft 级条目不出现在应用列表 | 明确 | 已实现 |
| R11 | **一致性审计** `--audit`：对照账本查单章冲突/漏记，只读不改 | P0 | Given 账本与第 N 章正文存在，When 执行 `--audit --chapter N`，Then 落盘 `chNNN.一致性审计.md`，且账本与正文的 mtime 均不变 | 明确 | 已实现（实测 ch6 抓出 7 处） |
| R12 | **冒烟测试**：`smoke_test.py` 零网络零依赖回归 | P1 | Given 改过任意引擎代码，When 执行 `python scripts/smoke_test.py`，Then 输出 `47 通过 / 0 失败` + `全部通过 ✅` | 明确 | 已实现（47 断言；Web 路由另见 R33，21 断言） |
| R13 | **多模型 failover**：主通道连续失败后自动切 `FALLBACK_*` 通道 | P1 | Given `.env` 配了 FALLBACK 三件套且主通道 key 非法，When 调用引擎，Then stderr 打印 `⚠ 主模型 … 失败，切换备用模型 …` 并返回正常正文；**请求体里的 model 必须跟着切成备用模型名** | 明确 | 已实现（含 deai 请求体 model 不切换的 bug 修复） |
| R14 | **推理失控抑制**：所有请求带 `reasoning_effort=low` | P1 | Given 使用带 reasoning 的模型，When 抓请求体，Then `reasoning_effort` 字段存在且为 low；实测思考 token 从 12000 降到 419 量级 | 明确 | 已实现 |
| R15 | **输入瘦身**：账本更新/审计对 >3200 字正文做首尾各 1400 字截断 | P1 | Given 正文 5000 字，When 调 `--state-only` 或 `--audit`，Then 输入 token 数回落到 5k 安全区（实测 6021→4238）且不再返回空内容 | 明确 | 已实现 |
| R16 | **账本补救** `--state-only`：只重算账本不重写正文 | P1 | Given 第 N 章正文已存在但账本漏更，When 执行 `--state-only --chapter N`，Then 账本刷新且 `chNNN.md` 内容字节不变 | 推断 | 已实现 |
| R17 | **调试开关** `--no-state`：写正文跳过账本更新 | P1 | When 带 `--no-state` 写章，Then 正文落盘且打印 `（--no-state：跳过账本更新）`，账本 mtime 不变 | 推断 | 已实现 |
| R18 | **零第三方依赖**：除 Python 标准库外不 import 任何包 | P1 | Given 一台只装 Python 的干净机器，When 跑通 README 快速开始 6 步，Then 不出现 `ModuleNotFoundError` | 承诺（README） | 已实现 |
| R19 | **OpenAI 兼容多后端**：`--base-url/--model/--key` 或 `.env` 可切任意厂商，配置链为 命令行 > .env > 内置默认 | P1 | Given `.env` 写了 `AGNES_BASE_URL`/`AGNES_MODEL`，When 不带命令行参数跑引擎，Then 生效的是 `.env` 的值（已修 argparse default 吞配置 bug） | 明确 | 已实现 |
| R20 | **密钥与产物隔离**：`.env`、章节正文、`*.bak.md`、diff、审计报告不入库 | P1 | Given 执行 `git status`/`git ls-files`，Then 仓库内无 `.env`、无 `chapters/`、无 `.bak.md` | 明确 | 已实现（`.gitignore` 用 `**/chapters/` + `*.bak.md`） |
| R21 | **Web 后端 API**：`http.server` 自写 JSON 路由，桥接两个引擎子进程 | P1 | Given 服务已起，When `curl http://127.0.0.1:PORT/api/status`，Then 返回 `{"ok":true,"key_set":…}`；书列表/读写 doc/读写章节/引擎动作各路由均通 | 明确 | 已实现 |
| R22 | **Web 三栏前端**：书树 → 编辑区 → 动作按钮 | P1 | Given 浏览器打开首页，When 点某书某章，Then 正文可编辑、可保存（PUT 回写），按钮触发 write/audit/scan/polish/apply | 明确 | 部分实现（Markdown 简版渲染；章节页动作条等前端缺陷见实施清单 P0，2026-09-03 待修） |
| R23 | **Web 新建书**：复制 `sample_book` 三件套建新书 | P1 | Given 输入合法书名，When POST `/api/books`，Then `books/<name>/` 下出现 设定/角色卡/大纲 三件套；同名或含 `/`/`\` 的书名返回 400 | 推断 | 已实现 |
| R24 | **AI 对话建书向导** `/api/book/from-chat`：聊出三件套 → 建书 → 自动 init-state | P1 | Given 多轮对话产出 设定/角色卡/大纲，When POST `from-chat`，Then 新书目录生成、三件套落盘、`story_state.md` 由引擎自动生成（端到端已跑通） | 明确 | 已实现 |
| R25 | **设置页**：`.env` 白名单键（6 个 `AGNES_*`/`FALLBACK_*`）读写 + 连通性 ping 测试 | P1 | Given 打开 ⚙ 设置，When 改 key 并点测试，Then `/api/settings/test` 返回 `{"ok":true,"model":…}` 且改完**无需重启服务**即生效（live_env 实时读） | 明确 | 已实现 |
| R26 | **流式对话 SSE**：`/api/chat` 带 `stream:true` 时逐 token 推 `event: delta` | P1 | Given 前端发起流式请求，When 模型吐字，Then 前端逐段收到 `event: delta` 并以 `event: done` 结束 | 明确 | 已实现 |
| R27 | **路径穿越防护**：书目录名与静态资源路径均做白名单校验 | P1 | Given 请求 `/api/book/../..` 或 `/static/../../.env`，Then 返回 403/404，读不到 books/ 之外任何文件 | 推断 | 已实现（`book_path()` 正则 + `serve_static` 前缀校验） |
| R28 | **Windows 可用**：提供 `start-web.bat`，控制台编码兼容 | P1 | Given 双击 bat，When 服务起来，Then 浏览器能打开；旧控制台（GBK）设 `PYTHONUTF8=1` 后脚本不崩 | 明确 | 已实现（bat 端口已统一 8801，见 04-Q9） |
| R29 | **发布体检**：连载节奏、读者反馈回收 | P2 | Given 一本连载中的书，When 跑发布体检，Then 给出更新节奏建议与读者反馈聚合视图 | 承诺（README 路线图） | 未实现 |
| R30 | **双端 UI / 手机审稿 + 新书模板库 / 大纲共创向导** | P2 | 移动端可审稿批注；模板库可一键套用类型模板 | 承诺（README 路线图） | 未实现 |
| R31 | **章节字数自定义与软控**：`--words N`（1000–10000，默认 3000）+ 超目标 ±30% 提醒；Web 向导可选字数。〔2026-09-04 拍板：超 ±30% 时提醒，并提供「加长/精简」一键按钮（接受二次模型调用成本）〕 | P1 | 待实现批次细化 GWT（目标=字数可调且失控能预警，非硬卡） | 推断+拍板 | **已实现**（v0.2，2026-09-04） |
| R32 | **提纲闸口（两段式 + 200 字试写）**：先出本章提纲 → 人工改 → 再写正文；开书含风格试写确认。〔2026-09-04 拍板：①**每章都确认**；②章纲固定结构 = 本章目标 / 关键事件 / 末尾钩子 + **伏笔操作行**（本章埋/推进/回收哪条）；③章纲与 200 字试写**一次生成、一起确认**；④Web 确认弹窗优先，CLI 同步加参数（如 `--plan`）；⑤配套「轻量多角色」流水线（策划/写手/审校）**每角色可配模型**（设置页 6→9 键，策划/审校留空回落写手，写手必填）；⑥提示词/词表/账本规矩**外置到 rules/ Markdown**（改规则不改代码），随本条落地 | P1 | 待实现批次细化 GWT | 明确+拍板 | **已实现**（v0.2，2026-09-04；⑤设置页 6→8 键 PLANNER_MODEL/REVIEWER_MODEL 留空回落写手，引擎 --plan/--audit 已按角色模型调用；⑥rules/*.md 外置 deai_rules/system/state_updater/audit/plan 五件，存在即覆盖内置） |
| R33 | **Web 路由验收测试**（`web/test_routes.py`，21 断言，本机回环） | P1 | 改 `server.py` 后必跑 | 承诺 | 已实现（含建书成功路径+shutil 防复发） |
| R34 | **一键导出/备份全书**：三件套+账本+正文+报告 → zip/md。〔2026-09-04 拍板：①zip 为**全家桶**（三件套+账本+正文+体检/审计报告+账本快照），解压即全量还原；②存项目内 `backups/` 目录，**保留最近 10 份**；③触发 = 手动「📦 备份全书」按钮 + 可选开关（每章写完自动 zip）；④配套规矩：每周手动拷一份到网盘/移动硬盘〕 | P1（高） | 待实现批次细化 GWT（目标=可完整还原） | 推断+拍板 | **已实现**（v0.2，2026-09-04；③每章自动备份开关=--auto-backup/写章请求 auto_backup 参数） |
| R35 | **伏笔生命周期状态机 + 超期告警**：账本内 planted→progressing→resolved/abandoned，超预计章数 3 章告警；并入"定稿自动反写校验设定库"(F-8)。〔2026-09-04 拍板：①「已失效」须**模型建议 + 作者确认**，不许自动标；②超期 3 章告警在**章纲确认页顶部红条**提示（出章纲时自动检查）；③常驻伏笔面板（右栏常挂列表）另列 v0.3〕 | P1 | 待实现批次细化 GWT | 推断+拍板 | **已实现**（v0.2，2026-09-04） |
| R36 | **拆书/导入已有稿续写**：粘贴旧稿 → 拆大纲/角色 → 接着写 | P2 | 待实现批次细化 GWT | 推断 | 未实现（v0.3） |
| R37 | **"学自己文风"**：本人样本段落 → 风格注入续写（不模仿名家，版权红线 O9） | P2 | 待实现批次细化 GWT | 推断 | 未实现（v0.3） |
| R38 | **角色/组织关系图谱**（Web 轻量版可视化） | P2 | 待实现批次细化 GWT | 推断 | 未实现（v0.3） |
| R39 | **可选向量召回层**：装 chromadb 后启用历史细节召回；默认项目仍零依赖 | P2 | 待实现批次细化 GWT（数据仍本机） | 明确（拍板） | 未实现（v0.3） |
| R40 | **轻量卡片化设定**：角色/物件单实体一文件 + 固定小标题字段，AI 按模板生成（不做完整 Schema/图谱重工程） | P2 | 待实现批次细化 GWT | 明确（拍板） | 未实现（v0.4） |
| R41 | **平台视角 AI 味自检**：低质特征（桥段同质/节奏均匀/对话模板/无意义环境描写）+ 创作痕迹完备度，按平台参数化 | P1 | 待实现批次细化 GWT | 推断 | 未实现（v0.3） |
| R42 | **一致性审计异构第二模型交叉复核**（主/备厂商不同模型，同模型看不出自己漂移） | P1 | 待实现批次细化 GWT | 推断 | 未实现（v0.3） |
| R43 | **模拟读者试读**：目标读者画像审章，输出弃书点与期待 | P2 | 待实现批次细化 GWT | 推断 | 未实现（v0.4） |
| R44 | **去 AI 味前后对比分**（改前 vs 改后量化，配合 M7 盲测） | P2 | 待实现批次细化 GWT | 推断 | 未实现（v0.4） |
| R45 | **账本体积监控**（>6000 字告警，防慢性膨胀） | P1 | 待实现批次细化 GWT | 推断 | 未实现（随时可做） |
| R46 | **创作痕迹链 / 自证材料包导出**：全链路（大纲→章纲→草稿→修订→定稿）自动留痕；一键导出"创作说明 + 修改留痕 + AI 参与环节（灵感/大纲/校对/润色/续写/直出）"供平台申报与误判申诉自证 | P1 | 待实现批次细化 GWT | 明确（拍板升级） | 未实现（v0.3，随 R41 平台自检同批） |
| R47 | **账本按章快照（回溯地基）**：每写完一章自动存 `_snapshots/chNNN.state.md`（当时账本的完整副本）；**永不清理**（单份几 KB，999 章也占不满 1MB） | P1 | Given 写完第 N 章，When 查 `_snapshots/`，Then 存在内容与当章账本一致的快照文件 | 明确（拍板 2026-09-04） | **已实现**（v0.2，2026-09-04） |
| R48 | **模型用量记账**：每次模型调用记流水（时间/动作/模型/输入输出 token）+ 按天汇总；Web 每次动作完成后显示「本次消耗 X tokens」；设置页加「📊 用量」小卡（累计 + 每章条目 + 平均每章消耗）；本地估算，不查厂商余额接口、暂不折算人民币 | P1 | 断网可查历史流水；写章后 Web 结果区可见本次消耗；账本文件随书存放 | 明确（拍板 2026-09-04） | **已实现**（v0.2，2026-09-04） |
| R49 | **账本重算（回溯执行）**：修改前文第 N 章后，从第 N 章快照起逐章重算其后所有章节的账本至最新（**正文不动**），一步到底执行，完成后出报告（列出重算范围 + 发现的待裁决矛盾） | P1 | 重算完成后各章账本 7 小节结构完整；报告明确列出 `⚠冲突` 待裁决项 | 明确（拍板 2026-09-04） | 未实现（v0.3） |

> R31-R46 为 2026-09-03 拍板新增 backlog（R 编号接续 R30）；验收 GWT 在各自实现批次排定时细化，避免 backlog 阶段过度设计。其中 R32 对应原补登"提纲闸口"（由 P2 提升 P1）；R39 对应原补登"向量检索记忆层"（定为可选插件形态）；R46 由平台情报调研的 F-7"AI 使用台账"升级而来（2026-09-03 晚拍板，作为作者"实质主导+留痕自证"的落点）。
>
> **R47-R49 为 2026-09-04 拍板新增**（概念学习第⑤站"回溯"与第①站"模型欠账"讨论产出）；R31-R35 的实现细节已于同日拍板补注（闸口每章确认、章纲结构、备份全家桶、伏笔失效双人确认等，见各条〔拍板〕标注）。v0.3 另拍板：对话式入口（AI 助手可调引擎、人闸口不变）、常驻伏笔面板、Step 引导条 + Tab 化界面美化——已登记进 docs/08 §4 v0.3 范围，不占 02 主表编号。

---

## 2. 路线图中已承诺但未列入上表的功能（补充登记）

以下 4 项为代码/日志中明确记录的**未开工缺口**，不占上表条目编号，供排期时取用：

| 缺口 | 出处 | 建议优先级 |
|---|---|---|
| ~~**提纲闸口（两段式）**~~ → 已升级为 **R32**（P1，v0.2 末/v0.3 初，含 200 字试写） | 记忆日志·Web v1 待办；README 原理第 2 节仍为"直接续写" | ~~P2~~ → P1 |
| ~~**向量检索记忆层**~~ → 已升级为 **R39**（可选插件形态，v0.3） | 记忆日志·GitHub 调研（对标 AI_NovelGenerator 6k★） | ~~P2~~ → P2（插件） |
| **写章进度轮询**：Web 写章目前单请求同步长等（1–3 分钟转圈） | 记忆日志·Web v1 待办 | P2（保留待办） |
| **前端 Markdown 完整渲染**：当前为简版渲染 | 记忆日志·Web v1 待办 | P2（保留待办） |

> 2026-09-03 更新：前两行已升级入主表（R32/R39），本补登区仅保留未升级的待办。

---

## 3. 优先级占比核算

| 优先级 | 条目数 | 占比 |
|---|---|---|
| **P0** | 11（R1–R11） | **36.7%** |
| P1 | 16（R12–R28） | 53.3% |
| P2 | 3（R29–R30） | 10.0% |
| **合计** | **30** | **100%** |

> 2026-09-03 重构注：新增 R31-R45（15 条 backlog）后条目总数 30→45；P1 新增 R31/32/33/34/35/41/42/45，P2 新增 R36/37/38/39/40/43/44（R33 已实现）。占比明细待下次核算刷新，P0（R1-R11）占比口径不变。

> **结论：P0 占比 36.7%，低于 40% 控制线**——核心闭环只锁了 11 条（连写 + 账本建更防幻觉 + 去味三动作 + 审计），其余 19 条（含 failover、冒烟测试、Web 工作台、AI 建书向导、设置页、流式对话等已交付能力）全部下沉到 P1/P2，说明这是一个"最小闭环先立住、工程配套后补"的立项结构。

---

## 4. 已知限制（记录在案，不修）

| # | 限制 | 出处 |
|---|---|---|
| L1 | L1 扫描时，**同一行内 hard 词出现多次只记 1 次** | 记忆日志·审计 L8 |
| L2 | `--apply` 对同一行多处命中**只替换首处** | 记忆日志·审计 L8 |
| L3 | 账本更新/审计对正文做**首尾截断**，正文中段事实可能漏入账 | `write_chapter.py` 注释（`……（正文中段已省略）……`） |
| L4 | 账本为**单层结构**，无向量检索，极长篇（>50 章）召回能力未验证 | 记忆日志·GitHub 调研结论 |
