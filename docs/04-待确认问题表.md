# 04 · 待确认问题表（novel-ledger）

> 本文档为项目复盘补写，事实依据见 README/代码/记忆日志（2026-09-03 工作日志）。
> 表头：编号 / 问题 / 谁确认 / 需答复时间 / 不答复的影响 / 状态。
> 状态取值：**待拍板**（需拍板人决定）/ **待核实**（需项目负责人查代码或跑一遍）/ **已搁置**（记录了但本期不动）。

---

## 1. 主表

| 编号 | 问题 | 谁确认 | 需答复时间 | 不答复的影响 | 状态 |
|---|---|---|---|---|---|
| Q1 | **示例书《雾城档案》正文是否随仓库公开？** 现状：`git ls-files` 显示入库的是 设定/大纲/角色卡/`story_state.md` 四件，7 章正文（约 3.5 万字）被 `.gitignore` 的 `**/chapters/` 隔离在本地。 | 拍板人 | `[待确认：下次发布前]` | 仓库只有"账本"没有"成品"，外界无法判断真实成书质量，开源说服力打折；若决定公开，需同步改 `.gitignore` 并复查正文有无敏感内容 | 待拍板 |
| Q2 | **Web 工作台是否部署公网 / 多人使用？** 现状：后端 `ThreadingHTTPServer` 绑 `127.0.0.1`，无账号、无会话、无并发用户概念；README 也未提部署方式。 | 拍板人 | `[待确认]` | 直接决定要不要补鉴权、会话、并发、速率限制、权限粒度——工作量差一个量级。不答复则默认"永远单机"，O1/O4/O5/O13 边界维持 | 待拍板 |
| Q3 | **提纲闸口（两段式：先出本章提纲 → 人工改 → 再写正文）是否纳入本期？** 现状：引擎是"直接续写"，Web v1 待办里挂着 plan 按钮未集成。对标 AI_NovelGenerator（6k★）是"每章提纲 → 草稿 → 定稿"三段。 | 拍板人 | `[待确认]` | 决定 P2 排期先后；也是作者端最想要的"可控感"来源，不答复则维持"AI 直接出成稿、人只能事后改" | 待拍板 |
| Q4 | **向量检索记忆层何时做？是否允许打破零第三方依赖约束？** 现状：记忆是单层滚动账本，无向量召回；50 章以上未验证。 | 拍板人 | `[待确认]` | 决定长篇上限。若要做，须先解除约束 C1（纯标准库），等于改技术栈基线 | 待拍板 |
| Q5 | **成功标准如何量化？** 现状：一页纸 PRD 只定了三条标尺（一致性错误·万字、AI 特征词频、真人盲测区分率 ≤50%），**没有任何数字**。01 文档 M1–M4、M7、M8 全部标 `[待确认]`。 | 拍板人 | `[待确认：建议 2026-09-10 前]` | 验收无标尺，只能"看着行"；M7 盲测（需定样本量、评委来源、评判维度）根本无法启动 | 待拍板 |
| Q6 | **是否清理 `books/测试/` 残留书？** 现状：`books/` 下除《雾城档案》外还有一个 `测试` 目录（仅有三件套、无章节、无账本，未入库）。 | 项目负责人 | `[待确认：下次整理时]` | 本地目录脏；若哪天把 `books/` 整体入库，会把空壳测试书一并公开 | 待核实 |
| Q7 | **`chapters/` 产物是否归档到子目录？** 现状：正文 `chNNN.md`、备份 `chNNN.bak.md`、diff `chNNN.AI腔diff.json`、报告 `chNNN.AI腔体检.md`、`chNNN.一致性审计.md` **全部平铺在同一个 `chapters/` 目录**，另有历史目录 `chapters/_bak_v1_截尾记忆/`。 | 项目负责人 | `[待确认]` | 章数一多（>30 章 × 5 类产物 = 150+ 文件）目录不可读；`_bak_v1_截尾记忆/` 是废弃的 v1 截尾记忆版本，留着会误导 | 待核实 |
| Q8 | **`web/` 目录为何未提交进 git？** 现状：仓库 15 个文件里没有 `web/`（server.py + static 三件套 + start-web.bat 均未在 `git ls-files` 中），但 README 已宣传"本地 Web 工作台"。最近一次 commit 是 2026-09-03 14:34（`8e27c08`），Web v1 在其后完成。 | 项目负责人 | `[待确认：建议下次 commit 前]` | 对外承诺不兑现：clone 下来没有 Web 工作台。属 P1 级别的开源完整性缺口 | 待核实 |
| Q9 | **`start-web.bat` 端口文案与实际不一致**：bat 里 echo 提示 `http://127.0.0.1:8899`，实际执行的是 `web\server.py --port 8801`（v2 因 8899 旧进程杀不掉而迁移）。 | 项目负责人 | `[待确认]` | 用户按提示打 8899 打不开，第一印象即"跑不起来" | 待核实 |
| Q10 | **AI 腔指数基线口径不一致**：记忆日志两处记录冲突——一处写"ch1–ch5 指数 0.5 / 1.4 / 0.3 / 0.6 / 1.8，apply 后 ch5 1.8→0.6、ch4 0.6→0.2"，另一处写"全书指数最高 ch2=1.0"。按前者 ch2=1.4，按后者 ch2=1.0，且 apply 前后口径混记。 | 项目负责人 | `[待确认]` | M2 无权威基线，后续改词表后无法判断是变好还是变差；指数定义（是否含 soft、是否含标点）需一并钉死 | 待核实 |
| Q11 | **GitHub PAT 是否吊销重签？** 记忆日志安全提醒：classic PAT 在本会话中明文出现过，30 天有效，建议 push 后手动删除并重新生成。 | 拍板人 | `[待确认：越快越好]` | token 明文留在会话历史里即泄露面；30 天有效期内可被用于写仓库 | 待拍板 |
| Q12 | **Agnes 通道的免费额度/限流上限是多少？长期资费如何？** 现状：主模型 `agnes-2.5-flash`，当初改用它的重要理由是"免费全模态"；额度、QPS、是否长期免费均无书面依据。 | 拍板人 | `[待确认]` | 直接决定能否稳定跑长篇（一本书 7 章已消耗 `[待确认]` 次调用）；一旦限流或收费，需回落到智谱/DeepSeek/Qwen 备选，成本模型要重算 | 待拍板 |
| Q13 | **账本更新"首尾截断"的漏记风险是否接受？** 现状：`update_state` 对 >3200 字正文只喂首尾各 1400 字，中段事实可能漏入账（代码里用 `……（正文中段已省略）……` 标注）。这是为绕开 reasoning 失控做的工程妥协。 | 拍板人 | `[待确认]` | 若不接受，需换方案（分段摘要再合并 / 换模型 / 提高阈值实测），是一笔独立的工程量 | 待拍板 |
| Q14 | **本期之后是否继续投入？P2（发布体检）/ P3（双端 UI）还做不做？** 现状：README 路线图挂着 P2、P3 未开工；GitHub 调研结论是"本项目 vs 6k★ 工作台差 3-4 个数量级工程量"。 | 拍板人 | `[待确认]` | 决定要不要续排里程碑；不答复则项目停在"最小可行研究原型"定位，README 路线图需改成"暂缓"以免误导 | 待拍板 |

---

## 2. 已搁置（记录在案，本期不动）

| 编号 | 事项 | 出处 | 为什么搁置 |
|---|---|---|---|
| Q15 | 分支名统一（是否从 `master` 改 `main`） | 记忆日志·开源前检查 | 仓库已是 `main`，无需再动 |
| Q16 | LICENSE 版权人署名 | 记忆日志·开源前检查 | LICENSE 文件已存在（MIT），署名名待拍板人定，不影响 MIT 生效 |
| Q17 | L1 同行 hard 词多次只记 1 次、`--apply` 只锚首处 | 记忆日志·审计 L8 | 已知限制，收益低于改动风险，记录不修 |
| Q18 | Windows 旧控制台 GBK 打印崩溃 | README 踩坑 11 | 已有绕行方案（`PYTHONUTF8=1`），不值得改脚本 |
