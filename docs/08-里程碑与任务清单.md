# 08 · 里程碑与任务清单（novel-ledger）

> 本文档为项目复盘补写，事实依据见 README/代码/记忆日志（2026-09-03 工作日志）+ git 提交记录。
> 说明：本项目从调研到开源**压缩在 2026-09-03 单日内完成**，多数节点只有"上午/中午/下午"粒度，**具体时分一律标 `[未记录]`，不补写假时间**。

---

## 1. 里程碑表（M0–M4）

| 里程碑 | 目标 | 关键产出物 | 计划完成 | 实际完成 | 状态 |
|---|---|---|---|---|---|
| **M0** 调研与立项 | 摸清赛道空位、定技术栈与 MVP 范围、拍板立项 | ① 30 项目全景调研（HTML）② 一页纸 PRD（定位/红线/MVP/技术栈/验收标尺）③ 4 项决策：立项 / Python+纯文件存储 / P0 五件套 / 本地开源部署 | `[未记录]`（原方案整体预估 6–8 周） | **2026-09-03 上午** | ✅ 已完成 |
| **M1** P0 配方验证 | 证明"上下文注入配方"能让大模型一章一章连写不崩 | ① `scripts/write_chapter.py` 初版（零依赖 urllib）② `sample_book` 三件套模板 ③ `books/雾城档案` 三件套 + ch1–ch3 ④ 踩坑清单（推理失控 / 低版本模型不守边界 / 道具漂移） | `[未记录]` | **2026-09-03 中午**（ch1–ch3 全新重跑成功，4846 / 4528 / 3262 字） | ✅ 已完成 |
| **M2** P1 记忆中枢 | 治长篇失忆：滚动状态账本取代每章 meta | ① `STATE_TEMPLATE` 7 小节 + `STATE_UPDATER_PROMPT` 9 条规则 ② `--init-state` / `--state-only` ③ ch4（5516 字）/ ch5（5622 字）验证：时间不倒退、待续状态接上、物件零漂移、伏笔自动标记"已回收" | `[未记录]` | **2026-09-03**（ch4 验收通过、ch5 长连载压测收官，具体时分 `[未记录]`） | ✅ 已完成 |
| **M3** P1 去 AI 味引擎 | 检测 + 改写 + 生成端预防，三层闭环 | ① `scripts/deai.py`（`--scan` / `--polish` / `--apply`）② L1 词表（hard/soft 两级 + 标点密度 + 跨章意象）③ L2 精判 + `AI腔diff.json` ④ 7 条 `DEAI_RULES` 注入 SYSTEM_PROMPT ⑤ 词表实战校准 3 处 + 引号锚点 bug 修复 | `[未记录]` | **2026-09-03**（全书 hard 硬伤清零，ch6 指数 0.2 为全书最低，具体时分 `[未记录]`） | ✅ 已完成 |
| **M4** 工程加固 + 开源 + Web 工作台 | 补三缺口、开源上线、给一个可视化人机入口 | ① 多模型 failover（两引擎）② `--audit` 一致性审计 ③ `smoke_test.py` 冒烟（23 断言）④ 独立审查 13 项缺陷全修 ⑤ GitHub `lesionmc/novel-ledger`（MIT，main，15 文件）⑥ `web/` 本地工作台 v1→v2（三栏 / AI 对话建书 / 设置页 / SSE 流式）⑦ README 沉淀（快速开始 / 命令手册 / 原理 / 12 条踩坑 / 路线图） | `[未记录]` | **2026-09-03**（首个 commit `8e27c08` 于 14:34；Web v1/v2 在 commit 之后，具体时分 `[未记录]`） | ✅ 已完成（**Web 未入库，见 Q8**） |

### 里程碑总览

| 项 | 值 |
|---|---|
| 计划周期 | 6–8 周（原方案 P0→P3 全量） |
| 实际周期 | 1 天（2026-09-03），且只做到 P0+P1，P2/P3 未开工 |
| 已交付里程碑 | M0–M4 全部完成 |
| 未开工 | P2 发布体检、P3 双端 UI、新书模板库 / 大纲共创向导 |

---

## 2. 已完成的 10 步实际进展（按真实发生顺序）

| # | 步骤 | 做了什么 | 关键结果 / 证据 | 时间 |
|---|---|---|---|---|
| 1 | **调研** | 桌面《AI小说赛道30项目全景与全流程产品方案.html》三连问 + 三轮体检 | 30 项目 / 7 类零件池；机会空位 = 共创入口 + 中文去 AI 味 + 原生双端；体检结论"值得写需求文档，先一页纸决策 PRD 逼拍板" | 2026-09-03（上午前，`[未记录]` 具体时分） |
| 2 | **方案** | 定位"中文·共创式·全流程·双端"辅助型写作台；五层架构（编排/记忆/技能/模型路由/发布体检）；阶段 P0→P4 | 明确红线：辅助非代写（番茄勾 AI 标识 / 起点 10% 下榜 / 晋江限校对级） | 2026-09-03 上午 |
| 3 | **立项** | 拍板 4 项决策 + 定代号 `ai-novel-workbench` + 初定智谱 GLM-5.3-Flash | 一页纸 PRD 定稿；验收标尺三条（一致性错误·万字 / AI 特征词频 / 真人盲测区分率 ≤50%） | 2026-09-03 上午 |
| 4 | **选型接入** | 改用 Agnes AI（`https://apihub.agnes-ai.com/v1`，OpenAI 兼容，Bearer）；项目落地到 `C:\Users\Administrator\Desktop\工作台\` | 主模型 `agnes-2.5-flash` + `reasoning_effort=low`（思考 12000→419 tokens） | 2026-09-03 中午 |
| 5 | **P0 配方验证** | v1 截尾 400 字记忆 → v2 七维结构化 `chXXX.meta.md` | v1 ch2 暴露 3 处穿帮（凭空编"潜入赵立新办公室"等）；v2 ch1–ch3 重跑（4846/4528/3262 字）衔接明显改善 | 2026-09-03 中午 |
| 6 | **P1 记忆中枢** | 废弃每章 meta，改全书唯一 `story_state.md` 滚动账本（覆盖式重写） | ch4 三项约束全中（时间顺延 / 待续状态接上 / 物件零漂移）；ch5 伏笔自动流转"已回收·第2章→第5章"、新埋 3 条伏笔；**5 章尺度零穿帮** | 2026-09-03（`[未记录]` 时分） |
| 7 | **P1 去 AI 味** | L1 词表 + L2 精判双级；`--apply` 安全应用；7 条红线注入生成端 | ch5 体检：5 条判 AI 腔、2 条正确放行（"不是…而是…"推理句）→ 证明 L1 召回、L2 过滤有效；ch6 红线验证 hard 3→0、指数 0.2 全书最低；全书 hard 硬伤清零 | 2026-09-03（`[未记录]` 时分） |
| 8 | **工程加固**（审计 / 冒烟 / failover） | ① 两引擎 `call_llm` 重构主备切换 ② `--audit` 只读审计（账本剥离伏笔节 + 正文截首尾）③ `smoke_test.py` ④ 独立审查 13 项缺陷全修 | 冒烟 18→**23 断言**全过；`--audit ch6` 真实抓出 3 冲突 + 2 漏记 + 2 存疑 = **7 处**；修掉 deai 请求体 model 不切换（H1）、`.env` 的 MODEL/BASE_URL 被忽略（M1）、choices 空数组 IndexError（M2）、`.format` 花括号崩（M3）等 | 2026-09-03（`[未记录]` 时分） |
| 9 | **开源 GitHub** | classic PAT 建仓 → `lesionmc/novel-ledger` Public；配 git http 代理绕过国内直连重置 | main 分支单 commit `8e27c08`（15 文件：LICENSE MIT + README + `.env.example` + `.gitignore` + `.gitattributes` + sample_book 三件套 + 雾城档案 4 件 + scripts 三脚本）；踩坑 5 条（连接器只读 403 / classic PAT / push 端点重置 / git 不走系统代理 / token 不写进 `.git/config`） | **2026-09-03 14:34** |
| 10 | **Web 工作台 v1 → v2** | v1：纯标准库后端 + 三栏前端（书树/编辑区/动作按钮），引擎子进程桥接；v2：AI 对话建书 + 设置页 + SSE 流式 + 自动 init-state | curl 自测全通（含路径穿越 404、新书保存回读、scan 零 token）；v2 from-chat 建书端到端跑通；端口 8899 → **8801**（8899 旧进程杀不掉）；踩 WinError 10013（bind 8765 被沙箱拦） | 2026-09-03（commit 之后，`[未记录]` 时分） |

> **关于 v3**：任务书提到 "Web 工作台 v1-v3"，但记忆日志与代码**只记录到 v2**（v2 = AI 对话建书 + 设置页 + SSE 流式）。v3 未见任何记录，`[未记录]`——不虚构。

---

## 3. 剩余任务清单

> 负责人默认：项目负责人 = lesionmc；需拍板人决定的事项标注在"依赖"列。截止时间均为 `[待确认]`（未与拍板人排期）。

| # | 任务 | 产出物 | 负责人 | 截止 | 依赖 |
|---|---|---|---|---|---|
| T1 | 应答 Q1–Q14 待确认问题（尤其 Q5 量化标尺、Q2 是否公网、Q11 token 吊销） | `04-待确认问题表.md` 状态列更新 | 拍板人 + 项目负责人 | `[待确认]` | 无 |
| T2 | 把 `web/` 提交进 git（server.py + static 三件套 + start-web.bat） | 一次 commit + 更新 README 快速开始 | 项目负责人 | `[待确认]` | Q8 确认；需复查无 `.env`/key 误入 |
| T3 | 修 `start-web.bat` 端口文案与实际端口不一致 | bat 文件 echo 改为 8801（或统一回 8899） | 项目负责人 | `[待确认]` | Q9 确认以哪个端口为准 |
| T4 | 统一 AI 腔指数基线口径并重跑一次全书 `--scan` 存档 | `docs/` 下基线快照 + 01 文档 M2 现状列更新 | 项目负责人 | `[待确认]` | Q10 确认口径 |
| T5 | 清理本地残留：`books/测试/`、`chapters/_bak_v1_截尾记忆/` | 删除或移入 archive | 项目负责人 | `[待确认]` | Q6 / Q7 确认；删除前先确认无引用 |
| T6 | 决定章节产物是否分目录归档（正文/报告/备份） | 目录结构调整 + `.gitignore` 同步 | 项目负责人 | `[待确认]` | Q7 确认 |
| T7 | **提纲闸口两段式**：先出本章提纲 → 人改 → 再写正文 | 引擎 `--plan` 动作 + Web plan 按钮 + 提纲落盘 | 项目负责人 | `[待确认]` | Q3 拍板纳入哪一期 |
| T8 | **写章进度轮询**：Web 写章从同步长等（1–3 分钟）改为异步任务 + 轮询 | 任务队列 + `/api/task/{id}` + 前端进度条 | 项目负责人 | `[待确认]` | T2（web 入库） |
| T9 | **发布体检（P2）**：连载节奏 + 读者反馈回收 | 体检报告模板 + CLI 动作 | 项目负责人 | `[待确认]` | Q14 确认是否继续投入；"读者反馈"数据源未定（O18 不对接平台） |
| T10 | **双端 UI（P3）**：移动端审稿 | 响应式改造 / PWA | 项目负责人 | `[待确认]` | Q14；可能触碰 C1 零依赖约束 |
| T11 | **向量检索记忆层**：账本 + 向量召回双通道 | 索引模块 + 召回注入 | 项目负责人 | `[待确认]` | Q4 拍板（是否解除零依赖约束） |
| T12 | **长连载压测**：把《雾城档案》从 7 章推到 `[待确认]` 章，验证账本在长尺度下不退化 | 压测报告（每章 `--audit` 条数 + 账本体积曲线） | 项目负责人 | `[待确认]` | Q12（Agnes 额度）；T4（先定基线） |
| T13 | **真人盲测（M7）**：验证区分率 ≤50% | 盲测方案 + 样本 + 结果报告 | 拍板人 + 项目负责人 | `[待确认]` | Q5（样本量、评委来源、评判维度） |
| T14 | 更新 README 路线图：标注 P2/P3 状态为"暂缓/待定"，避免对外误导 | README 路线图章节 | 项目负责人 | `[待确认]` | Q14 |
