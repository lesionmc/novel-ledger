# 10 · 验收清单（novel-ledger）

> 本文档为项目复盘补写，事实依据见 README/代码/记忆日志（2026-09-03 工作日志）。
> 本清单**逐条对应 `02-需求清单.md` 中的 P0 需求（R1–R11）**，P1/P2 不在本期验收范围。
> 验收方式取值：**演示**（跑一遍给人看）/ **测试**（跑程序断言）/ **数据核对**（比对文件内容或数字）。
> 结果取值：已通过 / 待验 / 未通过。验收人未落实的标 `[待确认]`。

---

## 1. P0 需求验收表

| 编号 | 对应需求 | 验收标准 | 验收方式 | 结果 | 验收人 | 日期 |
|---|---|---|---|---|---|---|
| A1 | **R1 单章连写** | Given 书目录含三件套且已建账，When 执行 `write_chapter.py --book X --chapter N`，Then 生成 `chapters/chNNN.md`，字数 ≥3000，控制台打印 `[3/4] 正文已落盘` | 数据核对 + 演示 | **已通过**（《雾城档案》ch001–ch007 共 7 章约 3.5 万字，单章 3000–5622 字） | `[待确认]` | 2026-09-03 |
| A2 | **R2 滚动状态账本 7 小节** | 打开 `story_state.md`，7 个 `##` 小节标题与 `STATE_TEMPLATE` 逐字一致：当前时间 / 计数与资源 / 角色状态 / 关键事件时间线 / 伏笔账本 / 关键物件 / 待续状态 | 数据核对 | **已通过**（`books/雾城档案/story_state.md` 7 小节齐全，已随仓库入库） | `[待确认]` | 2026-09-03 |
| A3 | **R3 建账 `--init-state`** | Given 新书只有三件套，When 执行 `--init-state`，Then 生成 `story_state.md`，当前时间=故事起点，无编造事实 | 演示 + 数据核对 | **已通过**（Web `from-chat` 建书链路内含自动 init-state，端到端跑通；《雾城档案》开书建账亦成功） | `[待确认]` | 2026-09-03 |
| A4 | **R4 账本自动更新（覆盖式重写）** | 写完第 N 章后账本整体刷新并打印 `账本已更新`；账本体积不随章数线性膨胀 | 演示 + 数据核对 | **已通过**（ch4 5516 字 / ch5 5622 字两轮均自动回填；ch5 主流程一气呵成约 60s `[待确认]`） | `[待确认]` | 2026-09-03 |
| A5 | **R5 账本防幻觉规则（冲突必须标注）** | 正文与旧账矛盾时，对应小节出现 `⚠冲突：旧账记X，本章写Y（请作者裁决）` 条目 | 测试（构造冲突样本）+ 数据核对 | **待验**（规则 9 已在 `STATE_UPDATER_PROMPT` 中固化，但**未留下触发该规则的实测样例**，需补一条构造用例） | `[待确认]` | — |
| A6 | **R6 账本只补不删（未了结伏笔保留）** | 连续写 5 章后，旧账"待回收"伏笔条目仍在（状态可改为"已回收·回收于第X章"），不得消失 | 数据核对 | **已通过**（ch5 账本中第 2 章"神秘监视者盯紧他"被自动标为"已回收·第2章→第5章"，并新埋 3 条伏笔） | `[待确认]` | 2026-09-03 |
| A7 | **R7 去 AI 腔红线（预防端）** | 红线注入后，同章 hard 命中数下降、破折号 <10 处/章 | 数据核对 | **已通过**（ch6：hard 命中 3→0、破折号由超标转为达标、副词堆砌清零、指数 0.2 为全书最低，ch1–ch5 为 0.5/1.4/0.3/0.6/1.8 `[待确认：见 Q10 口径冲突]`） | `[待确认]` | 2026-09-03 |
| A8 | **R8 L1 词表硬筛 `--scan`（零 token）** | 执行 `deai.py --scan <书目录>`，控制台输出每章指数与命中清单；断网可跑、不发任何网络请求 | 测试 + 演示 | **已通过**（本地零 token 全书体检跑通；Web 端 `scan` 自测亦通过） | `[待确认]` | 2026-09-03 |
| A9 | **R9 L2 精判 `--polish`（不改正文）** | 落盘 `chNNN.AI腔体检.md` + `chNNN.AI腔diff.json`，且正文 `chNNN.md` 修改时间不变 | 演示 + 数据核对 | **已通过**（ch5：L1 召回候选 → L2 判 5 条 AI 腔 / 2 条正常表达正确放行，证明"L1 召回 + L2 过滤"有效） | `[待确认]` | 2026-09-03 |
| A10 | **R10 安全应用 `--apply`** | 生成 `chNNN.bak.md` 备份；打印 `成功应用 N 处 / 跳过 M 处`；soft 级条目不出现在应用列表 | 演示 + 数据核对 | **已通过**（存量 5 章apply 后全书 hard 硬伤清零：ch004 1 处、ch005 2 处；ch5 指数 1.8→0.6、ch4 0.6→0.2） | `[待确认]` | 2026-09-03 |
| A11 | **R11 一致性审计 `--audit`** | 落盘 `chNNN.一致性审计.md`，且账本与正文 mtime 均不变（只读） | 演示 + 数据核对 | **已通过**（`--audit ch6` 真实抓出 **3 冲突 + 2 漏记 + 2 存疑 = 7 处**，含薄荷糖计数多记一颗、"沈浩/王浩"人名笔误、"攥布"线索漏入关键物件） | `[待确认]` | 2026-09-03 |

---

## 2. 汇总

| 结果 | 条数 | 编号 |
|---|---|---|
| 已通过 | **10** | A1、A2、A3、A4、A6、A7、A8、A9、A10、A11 |
| 待验 | **1** | A5 |
| 未通过 | 0 | — |
| **合计** | **11**（对应 P0 需求 R1–R11） | — |

> 2026-09-04 刷新：冒烟回归现引擎 **27 断言** + Web 路由 **16 断言**；示例书账本已恢复完整（R-09 关闭）；Web POST 路由已修复（R-13 关闭）。A5（冲突标注触发样例）仍待验，计划随 v0.2 伏笔状态机（R35）落地时一并补构造用例。

---

## 3. 待验项与阻塞原因

| 编号 | 阻塞原因 | 解除条件 |
|---|---|---|
| A5 | 账本冲突标注规则（规则 9）只有提示词层面的实现，**没有留下任何一次真实触发的记录或样例**，无法证明规则在实战中会生效 | 构造一个"正文与账本时间/计数矛盾"的样例跑一次 `--state-only`，把含 `⚠冲突` 的账本片段截图存档 |

---

## 4. 验收前置条件（尚未满足）

| # | 前置条件 | 说明 |
|---|---|---|
| 1 | **量化标尺未定**（Q5） | 一页纸 PRD 只给了三条标尺方向，M1–M4 / M7 / M8 全部无数字，本次验收只能用"是否跑通 + 实测数字"代替"是否达标"〔2026-09-03 晚更新：量化目标已拍板，见 01 §5 补丁〕 |
| 2 | **AI 腔指数基线口径冲突**（Q10） | A7 引用的 ch1–ch5 指数与记忆日志另一处"全书最高 ch2=1.0"不一致，需先钉死口径再复测〔2026-09-03 晚更新：口径定为 01 §5 M2 三档；权威基线重测仍待办（04-Q10）〕 |
| 3 | **验收人未落实** | 目前所有 P0 均由开发者自测，无独立验收人；建议至少 M6（新人不看文档跑通 README）与 M7（真人盲测）由第三方执行 |
| 4 | **冒烟测试（R12，P1）不在本表** | 虽为 P1，但实测 **23/23 断言全过**，建议每次发版前必跑并留档〔2026-09-03 晚更新：引擎 27 + Web 路由 16〕 |

---

## 5. 建议的回归动作（每次改代码后）

| 顺序 | 动作 | 通过判据 |
|---|---|---|
| 1 | `python scripts/smoke_test.py` | `23 通过 / 0 失败` + `全部通过 ✅` |
| 2 | `python scripts/deai.py --scan books/<书>` | 各章 hard 命中数不上升 |
| 3 | `python scripts/write_chapter.py --book books/<书> --chapter N --audit` | 审计报告条数不上升（新冲突数为 0 或可解释） |
| 4 | 跑完 `--apply` 后**必须 re-scan** | 防 AI 改 AI 引入新腔（README 踩坑第 8 条） |
