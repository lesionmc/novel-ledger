# 13 · 变更记录

本文档为项目复盘补写，技术事实以代码为准。
**时间说明**：仓库当前只有 **1 个提交**（`8e27c08`，2026-09-03 14:34）。表中时间一列，凡能由 `git log` 直接证实的标具体时刻；其余以**文件 mtime 推断**，标注 `[当天]` 或 `[推断]`。精确时点 [待确认]。

---

## 一、变更总表

| 编号 | 变更内容 | 提出人 | 日期 | 影响评估 | 决策 | 批准人 |
|---|---|---|---|---|---|---|
| CH-01 | 项目起盘：建立 `sample_book/` 三件套模板（设定/角色卡/大纲），示例书《雾城档案》立项 | 作者（本人） | 2026-09-03 `[当天 11:08 推断]` | 无（新建） | 通过 | 作者（本人） |
| CH-02 | 连载引擎 `scripts/write_chapter.py` 落地：上下文配方（宪章+大纲+角色卡+上一章结尾 800 字） | 作者（本人） | 2026-09-03 `[当天 上午]` | 无（新建） | 通过 | 作者（本人） |
| CH-03 | 生成 `ch001`–`ch003`（截尾记忆方案时期，产物后归档至 `chapters/_bak_v1_截尾记忆/`） | 引擎 | 2026-09-03 `[当天 11:43–11:45 推断]` | 中：暴露记忆穿帮 | 通过（后返工） | 作者（本人） |
| CH-04 | **方案级返工**：记忆机制从「文末摘要 + 截尾」改为**结构化滚动账本** `story_state.md`（7 小节、覆盖式重写） | 作者（本人） | 2026-09-03 `[当天 中午]` | 高：核心架构变更，旧章归档 | 通过 | 作者（本人） |
| CH-05 | 新增去 AI 味引擎 `scripts/deai.py`：L1 词表硬筛 + L2 AI 精判 | 作者（本人） | 2026-09-03 `[当天 12:4x 推断]` | 中：新增能力 | 通过 | 作者（本人） |
| CH-06 | 生成 `ch004`–`ch005`，并对两章跑 `--polish` / `--apply`（留下 `.AI腔体检.md`、`.AI腔diff.json`、`.bak.md`） | 引擎 | 2026-09-03 `[当天 12:40–12:44 推断]` | 中：验证去味链路 | 通过 | 作者（本人） |
| CH-07 | 故障修复：账本更新对正文**首尾截短**（>3200 字 → 各 1400），规避输入 >5k tokens 触发 reasoning 失控 | 作者（本人） | 2026-09-03 `[当天 ch4 实测后]` | 高：修复后账本更新可用 | 通过 | 作者（本人） |
| CH-08 | 生成 `ch006`，首次跑 `--audit` 一致性审计，产出 `ch006.一致性审计.md`（查出 4 类问题） | 引擎 | 2026-09-03 `[当天 13:02–13:05 推断]` | 中：验证审计链路 | 通过 | 作者（本人） |
| CH-09 | 去味引擎与连载引擎定版：`deai.py` 13:20、`write_chapter.py` 15:35（含 `reasoning_effort=low`、failover、`SENT_END` 含引号等修复） | 作者（本人） | 2026-09-03 `[当天 13:20 / 15:35 推断]` | 中：行为固化 | 通过 | 作者（本人） |
| CH-10 | 新增 `scripts/smoke_test.py` 零网络冒烟测试（23 条断言） | 作者（本人） | 2026-09-03 `[当天 13:22 推断]` | 中：建立回归能力 | 通过 | 作者（本人） |
| CH-11 | 缺陷修复：**切备用通道时必须替换请求体 `model` 字段**（原只换 URL 导致备用网关报模型不存在）；补 2 条断言守该行为 | 作者（本人） | 2026-09-03 `[当天 13:2x 推断]` | 高：failover 真正可用 | 通过 | 作者（本人） |
| CH-12 | 工程配套：`.gitignore`（排除 `.env`、`**/chapters/`、`*.bak.md`）、`.gitattributes`（统一 LF） | 作者（本人） | 2026-09-03 `[当天 12:46 / 13:21 推断]` | 高：防 key 入库 | 通过 | 作者（本人） |
| CH-13 | 新增 `LICENSE`（MIT） | 作者（本人） | 2026-09-03 `[当天 13:27 推断]` | 无 | 通过 | 作者（本人） |
| CH-14 | 新增 `README.md`（快速开始、命令手册、原理、踩过的坑 12 条） | 作者（本人） | 2026-09-03 `[当天 14:03 推断]` | 中：对外门面 | 通过 | 作者（本人） |
| CH-15 | 新增 `.env.example` 配置模板（Agnes / 智谱 / DeepSeek 三组 + `FALLBACK_*` 说明） | 作者（本人） | 2026-09-03 `[当天 14:06 推断]` | 中：换厂商门槛降低 | 通过 | 作者（本人） |
| CH-16 | **开源发布**：首次提交 `8e27c08 feat: novel-ledger v0.1`，推至 github.com/lesionmc/novel-ledger | 作者（本人） | 2026-09-03 14:34 | 高：里程碑 | 通过 | 作者（本人） |
| CH-17 | 修订示例书 `设定.md`（补硬性写作约束：时间线、称呼规则、能力代价）+ 生成 `ch007` | 作者（本人） | 2026-09-03 `[当天 15:21 / 15:37 推断]` | 中：提升事实稳定性 | 通过 | 作者（本人） |
| CH-18 | **Web v1**：新增 `web/server.py`（ThreadingHTTPServer + 手写路由）+ `web/static/{index.html,style.css,app.js}` 三栏工作台（书树/章节/报告 + 正文编辑 + 保存） | 作者（本人） | 2026-09-03 `[当天 16:0x–16:24 推断]` | 高：新形态，README 路线图未同步 | 通过（追加需求） | 作者（本人） |
| CH-19 | 新增 `start-web.bat`（`chcp 65001` + 固定路径 python + `--port 8801`） | 作者（本人） | 2026-09-03 `[当天 16:10 推断]` | 中：Windows 一键启动 | 通过 | 作者（本人） |
| CH-20 | **Web v2**：新增 ⚙ 设置页（`GET/PUT /api/settings`、测试连接）与 `POST /api/book/from-chat` AI 建书 | 作者（本人） | 2026-09-03 `[当天 16:2x 推断]` | 高：配置热生效 + 建书自动化 | 通过（追加需求） | 作者（本人） |
| CH-21 | 缺陷修复：静态文件 `Content-Type` 扩展名 key 去掉前导点（此前 `.js/.css` 被当成 `application/octet-stream`） | 作者（本人） | 2026-09-03 `[当天 Web v1 期间]` | 高：不修则页面白屏 | 通过 | 作者（本人） |
| CH-22 | 缺陷修复：`from-chat` 建书增加三件套完整性校验（此前缺键会 `KeyError` → 500） | 作者（本人） | 2026-09-03 `[当天 Web v2 期间 推断]` | 中：修 500 错误 | 通过 | 作者（本人） |
| CH-23 | **Web v3**：`app.js` 升至 v3——SSE 流式聊天（`POST /api/chat` + `stream:true`，后端 `event: delta/done/error`）+ 建书向导**从模态框改为嵌入式主视图** | 作者（本人） | 2026-09-03 `[当天 16:41 推断]` | 高：体验重构；**同时引入 2 个未修复缺陷**（见 CH-24/25） | 通过（追加需求） | 作者（本人） |
| CH-24 | **遗留缺陷（未处理）**：`server.py` 中 `POST /api/books`、`from-chat`、`write`、`audit`、`scan`、`polish`、`apply` 的路由代码缩进与 `_chat_stream()` 同级，落进其方法体内（`handle_api_post` 378–414 行，`_chat_stream` 416–575 行，AST 实测）→ 这批路由不可达 | 复盘发现（AI 助手） | 2026-09-03 `[当天 复盘时]` | 🔴 高：Web 核心功能不可用 | ✅ **已修复**（2026-09-03：路由上移进 `handle_api_post` + `test_routes.py` 16 断言守护，见 CH-28） | 作者（本人） |
| CH-25 | **遗留缺陷（未处理）**：`app.js` 模板字符串内 HTML 破损（`<input<type=`、`</textarea">`、`onclick="…"()`）+ `renderBrief()` 调用未定义函数 `$const(...)` | 复盘发现（AI 助手） | 2026-09-03 `[当天 复盘时]` | 🟡 中：设置面板与建书向导渲染异常 | 🟡 **部分修复**（2026-09-03：HTML 破损与 `$const` 已修；章节页 `chapterActions()` 未定义等 3 个前端 P0 仍待修，v0.2 首件） | 作者（本人） |
| CH-26 | **遗留缺陷（未处理）**：示例书 `story_state.md` 被模型输出截断（末行停在 `## 关键事件时间线（最近约 10`，缺第 4–7 节，19 行 918 字符） | 复盘发现（AI 助手） | 2026-09-03 `[当天 15:38 写入 / 复盘时发现]` | 🔴 高：记忆中枢残缺 | ✅ **已修复**（2026-09-03：账本恢复完整（commit `9f84edc`）+ `validate_state()` 自动校验，见 CH-28） | 作者（本人） |
| CH-27 | 文档补写：新增 `docs/` 技术类文档（05/06/07/09/11/12/13/14）与需求类、管理类文档 | 作者（本人） | 2026-09-03 `[当天 16:59 起]` | 中：补齐项目流程文档 | 通过 | 作者（本人） |
| CH-28 | **需求文档重构落盘**：01 M1-M8 目标量化拍板（+§6 v0.2 范围、§9 决策行）；02 修正 R1/R12/R22/R28 并新增 R31-R45 backlog（15 条）；03 新增 O21/O22 边界 + T 表状态刷新；04 Q1-Q14 状态刷新（5 拍板/3 已解决/2 待核实/1 安全待办）；13 记本条。另有引擎 P0 修复（账本 7 小节校验 27 断言）+ Web 路由验收（R33，16 断言）+ API Key 脱敏 | 作者（本人）+ AI 助手（问答拍板） | 2026-09-03 `[当天 22:0x]` | 高：需求文档从"复盘补写"转为"可执行目标+backlog" | 通过 | 作者（本人） |
| CH-29 | **v0.2 需求拍板落盘**（三轮问答 34 项，2026-09-04）：R31-R35 补实现细节（闸口每章确认、章纲=目标/关键事件/钩子+伏笔操作行、章纲+试写一次确认、备份全家桶留 10 份、伏笔失效=模型建议+人确认、字数一键加长/精简）；新增 R47 账本按章快照、R48 用量记账、R49 账本重算；规则外置 rules/ 随 R32 落地；压测目标=连写至 20 章（前置：先读大纲评估，不够出扩写草案）；发布策略=tag v0.2.0+Release+补 v0.1.0 tag；样例章挑 ch4/ch5；E2E=五条主流程全绿；盲测压测后启动；docs/15 补第③④⑤⑥站；本表 CH-24/25/26 状态刷新；docs/05/06/09/10/11/12/14 滞后状态同步 | 作者（本人）+ AI 助手（问答拍板） | 2026-09-04 | 高：v0.2 范围定稿，可开工 | 通过 | 作者（本人） |
| CH-30 | **真 bug 修复：server.py 漏 `import shutil`** → 建书/建书向导全部 500（作者实测抓到）；test_routes 补建书成功路径断言（教训：成功路径也要断言） | 复盘发现（AI 助手） | 2026-09-04 | 🔴 高：建书不可用 | ✅ 已修复（路由 21 断言） | 作者（本人） |
| CH-31 | **v0.2 功能批次实现**：R31 字数软控（--words/--adjust）+ R32 闸口（--plan 章纲落盘/build_context 注入/Web plan/plan-save）+ R34 一键备份（backup_book.py/backups/ 留 10 份）+ R35 伏笔超期（规则6强化/overdue 扫描/详情接口/闸口红条）+ React UI 7 页全覆盖（A′ 预构建 web/ui/）；冒烟 34→47、路由 19→21 | 作者（本人）+ AI 助手 | 2026-09-04 | 高：v0.2 功能层完工 | 通过 | 作者（本人） |

---

## 二、按影响等级汇总

| 等级 | 编号 | 共性问题 |
|---|---|---|
| 🔴 高 | CH-04, CH-07, CH-11, CH-12, CH-16, CH-18, CH-20, CH-21, CH-23, **CH-24, CH-26** | 高影响变更集中在「架构返工」与「Web 化」两波；**两条高危遗留缺陷都出在最后一波（v3）** |
| 🟡 中 | CH-03, CH-05, CH-06, CH-08, CH-09, CH-10, CH-14, CH-15, CH-17, CH-19, CH-22, **CH-25**, CH-27 | 常规迭代 |
| 🟢 低 | CH-01, CH-02, CH-13 | 初始化动作 |

## 三、变更规律（供下次参考）

| 观察 | 数据 | 含义 |
|---|---|---|
| 三个高峰 | ① 上午（引擎与示例书）② 13:00–14:34（工程配套 + 开源）③ 16:00–16:49（Web v1→v3） | 第三波（Web）是**一天内连续三版**，且没有任何一版有验收记录 |
| 缺陷密度与版本节奏强相关 | 引擎侧缺陷（CH-07/CH-11）都已修复并补了断言；Web 侧缺陷（CH-24/CH-25）**全部未处理** | 有回归手段的地方质量高，没有的地方质量低 |
| 「未同步」的文档债 | CH-18 之后 README 路线图仍标 Web「未完成」；`deai.py` docstring 仍写「--apply 留待下一步」；`server.py` docstring 少了 6 条路由 | 功能先行、文档滞后，最终要一次性补（即 CH-27） |

## 四、变更流程要求（摘要，详见 `14-完成定义与协作机制.md`）

| 规则 | 说明 |
|---|---|
| **可以换，不能只加** | 每加一项能力，必须回答「换来什么可以下线或简化」 |
| 变更必留证据 | 代码注释里写「现象 + 原因 + 解法」；文档同步更新本表 |
| 缺陷必须转断言 | 修复一个缺陷，同步在 `smoke_test.py` 加一条断言（或等价的验证步骤） |
| 未验收的功能不许对外宣称完成 | 如 Web 工作台在 CH-24/CH-25 修复前，README 不应标为可用 |

---

## 五、2026-09-05 全面体检修复批次

> 本批次源于一次全书体检，12 项问题清零：断言总量 83 → 91（179 条断言全部通过），
> 同期真书《雁回刀》35 章完结。

| 编号 | 变更内容 | 日期 | 验收断言 |
|---|---|---|---|
| CH-32 | 体检 `--full` 修复 + L1 词表统一 | 2026-09-05 | 12 |
| CH-33 | 服务端安全四件套 + 原子写 | 2026-09-05 | 18 |
| CH-34 | 任务系统超时 + 补漏 + usage 加锁 | 2026-09-05 | 15 |
| CH-35 | 备份分离 | 2026-09-05 | 9 |
| CH-36 | LLM 流式 + failover | 2026-09-05 | 21 |
| CH-37 | 路由表化（45 路由） | 2026-09-05 | 45 |

> 勘误（2026-09-06）：CH-37 的路由表化重构随当晚数据事故丢失，现版 server.py 为 if 链分发 + 全部安全修复；
> 路由表化重列为待办，不再视为已完成。
| CH-38 | 清债：CI 缓存 | 2026-09-05 | 6 |
| CH-39 | elapsed_ms 耗时统计 | 2026-09-05 | 8 |
| CH-40 | bench_longform 超长篇压测工具 | 2026-09-05 | 5 |

## 六、2026-09-05 数据事故与恢复记录

| 时间 | 事件 |
|---|---|
| 22:42 | 项目目录被整体删除。GitHub 重新 clone 后仓库仅剩 v0.1 的两个提交 |
| 事故清点 | 53 个提交历史、《雁回刀》35 章、《雾城档案》33 章、当日用量记录全部丢失 |
| 恢复 | 经 9/4 19:46 备份 rar 恢复 22 个提交 + 《雾城档案》32 章 + `.env` |
| 重建 | v0.6/v0.7 与 9/5 当日改动由 AI 凭会话记忆重建（守卫进程、桌面壳、Skill 薄壳、生态分发文档等）；WinFR 扫描并行进行，作为最后兜底 |
| 教训 | **修复完成后必须立即 `git push`；书稿（chapters/）必须入库**，`.gitignore` 不再排除 chapters/（见同日 .gitignore 调整） |
